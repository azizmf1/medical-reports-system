{
  "name": "medical-reports-system",
  "version": "1.1.0",
  "private": true,
  "description": "Medical Reports Management System - Dynamic Form Builder + Approval Workflow + Entity Sharing",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -k -n server,client \"npm run dev -w server\" \"npm run dev -w client\"",
    "seed": "npm run seed -w server",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
