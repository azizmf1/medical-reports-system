{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "if [ -d client ]; then cd client; fi && npm install",
  "buildCommand": "if [ -d client ]; then cd client; fi && npm run build && mkdir -p client && cp -r dist client/ 2>/dev/null || true",
  "outputDirectory": "client/dist",
  "framework": null,
  "rewrites": [
    { "source": "/((?!assets/).*)", "destination": "/index.html" }
  ]
}
